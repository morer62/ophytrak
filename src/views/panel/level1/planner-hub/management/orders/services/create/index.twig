{% extends 'templates/base.admin.twig' %}
{% import 'templates/macros.twig' as macros %}

{% block body %}

    <h1 class="h3 mb-4">
        <i data-feather="plus"></i> {{ trans('planner_hub.create_service') }}
    </h1>

    {{ macros.breadcrumb2() }}

    <section class="card">
        <div class="card-body">
            <form method="POST">
                <div class="mb-3">
                    <label class="form-label">{{ trans('planner_hub.service_name') }}</label>
                    <input type="text" name="name" class="form-control" required maxlength="50">
                </div>

                <div class="mb-3">
                    <label class="form-label">{{ trans('planner_hub.short_description') }}</label> 
                    <input type="text"  name="description" class="form-control"  maxlength="100" required >
                </div>

                <div class="mb-3">
                    <label class="form-label">{{ trans('planner_hub.external_url') }}</label> <small> {{ trans('planner_hub.more_details_promotions') }}</small>
                    <input type="text"  name="description_url" class="form-control"  maxlength="200"  >
                </div>

                <div class="mb-3">
                    <label class="form-label">{{ trans('planner_hub.variable_price') }}</label>
                    <div class="form-check">
                        <input type="checkbox" name="is_variable" id="is_variable" class="form-check-input" value="YES">
                        <label class="form-check-label" for="is_variable">
                            {{ trans('planner_hub.service_variable_pricing') }}
                        </label>
                    </div>
                </div>

                <div class="mb-3" id="price_field">
                    <label class="form-label">{{ trans('planner_hub.price_dollar') }}</label>
                    <input type="number" name="price" step="0.01" class="form-control" value="0.00" required>
                    <small class="text-muted">{{ trans('planner_hub.leave_zero_variable') }}</small>
                </div>

                <div>
                    <button class="btn btn-primary">💾 {{ trans('planner_hub.save_service') }}</button>
                    {{ macros.back_btn(title=trans('planner_hub.cancel')) }}
                </div>
            </form>
        </div>
    </section>

    <script>
        // Función para manejar la visibilidad del campo de precio
        function togglePriceField() {
            const isVariable = document.getElementById('is_variable');
            const priceField = document.getElementById('price_field');
            const priceInput = priceField.querySelector('input[name="price"]');
            
            if (isVariable.checked) {
                priceInput.value = '0.00';
                priceInput.readOnly = true;
                priceField.style.display = 'none';
            } else {
                priceInput.value = '';
                priceInput.readOnly = false;
                priceField.style.display = 'block';
            }
        }

        // Event listener para cambios en el checkbox
        document.getElementById('is_variable').addEventListener('change', togglePriceField);
        
        // Ejecutar al cargar la página para establecer el estado inicial
        document.addEventListener('DOMContentLoaded', togglePriceField);
    </script>
{% endblock %}